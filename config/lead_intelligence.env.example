# Phase 3F Canonical Lead Intelligence reader.
# Provision only after the dedicated login role receives a password.
EMPIRE_LEAD_INTELLIGENCE_DSN=

# This runtime is read-only by design.
# No fallback to service_role exists.
# The transport executes:
#   SET LOCAL ROLE empire_lead_intelligence_reader
# and permits only a fixed SQL query map.
