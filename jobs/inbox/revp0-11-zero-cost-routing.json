{
  "schema_version": "empire.hermes.control_job.v1",
  "job_id": "revp0-11-zero-cost-routing",
  "kind": "code_task",
  "authority": "observe",
  "base_branch": "feature/revenue-intelligence-v2",
  "prompt": "Verify the governed Hermes worker after commits ea91a1c0 and 089bcfbc. Do not modify files. Confirm the resident worker uses the live OmniRoute catalog, excludes paid models unless EMPIRE_HERMES_ALLOW_PAID_MODELS is explicitly enabled, walks zero-cost OpenRouter routes only, and uses the bounded isolated Hermes configuration. Run focused tests for hermes control, Hermes systemd contract, Resend inbound webhook, reply classifier, closer reply worker, revenue command queue and founder dashboard. Inspect read-only runtime evidence only. Do not send outbound, mutate production data, move funds, touch recovery/ or toop/, or infer revenue.",
  "allowed_paths": [
    "empire_os/",
    "tests/",
    "docs/"
  ],
  "pytest_targets": [
    "tests/test_hermes_control.py",
    "tests/test_hermes_control_systemd.py",
    "tests/test_resend_webhook_app.py",
    "tests/test_reply_classifier.py",
    "tests/test_closer_reply_worker.py",
    "tests/test_revenue_command_queue.py",
    "tests/test_founder_dashboard.py"
  ],
  "max_runtime_seconds": 1200,
  "created_at": "2026-09-23T17:36:00Z"
}
